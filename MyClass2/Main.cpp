#include <iostream>
#include "Auto.h"

using namespace std;

int main() {
    Auto one;
    Auto two;
    Auto three;

    one.acelerar(true);
    two.frenar(false);
    three.girar(true);

    return 0;
}